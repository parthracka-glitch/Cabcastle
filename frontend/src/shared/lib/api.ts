import apiClient from '../api/axios-client';

export { default, BACKEND_URL, API, formatApiError, formatINR, getOptimizedImageUrl, safeFormatDate, clearApiCache } from '../api/axios-client';
export * from '../api';
